<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:ListViewProject" 
             x:Class="ListViewProject.MainPage">
        <ListView x:Name="FlashCardList" ItemTapped="OnItemTappedAsync">
        <ListView.ItemTemplate>
          <DataTemplate>
                <!--<ImageCell Text="{Binding Question}" ImageSource="{Binding ImageName}"/>-->
                <!--<TextCell Text="{Binding Question}" />-->
                    <ViewCell>
                        <StackLayout Orientation="Vertical">
                            <StackLayout Orientation="Horizontal">
                                <Image Source="{Binding ImageName}" 
                                       HeightRequest="30"
                                       WidthRequest="30"/>
                                <Label Text="{Binding Question}"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
</ContentPage>